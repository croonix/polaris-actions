# Release workflow for polaris-actions — PLACEHOLDER.
#
# This is a minimal, non-broken placeholder for the scaffold task (plan
# 278-polaris-actions-repo-and-e2e, step E1). The real release logic —
# tag (v*.*.*) -> build -> verify dist/ freshness -> create GitHub Release
# -> force-move the v1 convenience tag via a GitHub App installation token
# (actions/create-github-app-token), environment-gated with a required
# reviewer — is specified in RFC 030 D-26 and built out in a later plan
# step (E2/E3). Do not tag v1.0.0 against this placeholder; the
# input/output name ratification gate (D-26) must happen first.
#
# SHA-pinned actions:
#   actions/checkout v7.0.1 → 3d3c42e5aac5ba805825da76410c181273ba90b1

name: Release

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Release (placeholder — not implemented)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Not implemented
        run: |
          echo "release.yml is a placeholder. Real release logic (D-26) lands in a later plan step."
          exit 0
